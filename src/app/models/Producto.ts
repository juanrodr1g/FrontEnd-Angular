export interface Producto {
    id?: number,
    nombre?: string,
    proveedor?: string,
    image?: string,
    created_at?: Date,
    descripcion?: string,
    stock?: number,
    precio?: number
}